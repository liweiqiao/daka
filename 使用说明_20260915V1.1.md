# 清城少年志 · 国庆打卡工具 使用说明

> 版本 V1.1 · 2026-09-15
> 面向 2026 国庆 7 天（10/1–10/7）心理品质打卡活动，预计 1000 名参与者。
>
> **V1.1 相比 V1.0 的变化**：数据库已通、建表完成、三套验收脚本全部跑通（49 + 58 + 44 项断言，
> 0 失败）；新增数据清理脚本 `db:reset`；补充"演练模式"、七牛未配置时的现状说明、以及两个
> 踩过的坑（本机代理、上传临时目录）。

---

## 零、当前进度（一眼看完）

| 项 | 状态 |
|---|---|
| 远程数据库 `120.79.240.81:3306/daka` | ✅ 已连通，**MySQL 5.7.40**（不是 8.0，SQL 已按 5.7 写过） |
| 8 张表 + 索引 | ✅ 已建 |
| 49 项任务字典（7 天 × 7 主题） | ✅ 已灌入，含 10/2–10/6 的线下打卡点标记 |
| 管理员账号 | ✅ `admin / admin123456`（**上线前必须改**） |
| 接口验收（`test/smoke.js`） | ✅ 49 项断言全通过 |
| 统计口径对账（`test/stats-audit.js`） | ✅ 58 项全对得上（与独立 SQL 交叉核对） |
| 浏览器端验收（`test/browser-check.js`） | ✅ 44 项全通过（真 Chrome 跑完整流程） |
| 当前库内数据 | ⚠️ **有假数据**：200 人演示数据 + 若干验收测试账号，正式上线前必须清（见第五节） |
| 文件存储 | ⚠️ 现在是 `local`（本机磁盘）。七牛凭证还没给，填上即可切（见第七节） |
| 微信分发所需 | ⚠️ 还缺一个公网 HTTPS 域名（见第六节） |

---

## 一、为什么不用问卷网，改自建

这不是"重复造轮子"，是被三件事逼的（都在问卷网方案的实测里踩过）：

| 问卷网的堵点 | 本工具怎么解决 |
|---|---|
| **上传题无法设为选填**：图片/文件题在提交时被判"文件无效"，必填项全填也没用 | 照片必填、视频选填是服务端显式校验，两者独立判定 |
| **免费版每月只有 1GB 上传空间，且不可增购** | 附件走七牛云或本机磁盘，空间自己说了算；后台有"导出+清理"循环使用的运维动作 |
| **没有去重能力**，同一人同一天能重复提交同一主题 | 数据库层唯一索引物理拦住，应用层只负责把错误翻成人话 |

另外问卷网无法做到"信息只填一次"—— 每次提交都要重填姓名学校。本工具用 token 识别参与者，登记一次管到底。

---

## 二、技术栈与结构

- **前端**：Vue 3 + Vite（参与者端 + 统计后台，同一份构建产物）
- **后端**：Node.js + Koa2
- **数据库**：MySQL 5.7（远程 `120.79.240.81:3306`，库名 `daka`）
- **文件存储**：可切换 —— 本机磁盘（当前）或七牛云

```
打卡工具/
├── server/                     后端
│   ├── src/
│   │   ├── index.js            服务入口（静态托管 + 路由挂载 + 启动自检）
│   │   ├── config.js           全部环境变量集中读取
│   │   ├── db.js               MySQL 连接池 + 事务封装
│   │   ├── auth.js             两套 JWT（参与者 / 管理员）
│   │   ├── time.js             北京时间口径（服务端定日期，不信客户端）
│   │   ├── http.js             统一响应格式与错误类型
│   │   ├── fsx.js              临时文件清理（带重试的删除 + 过期清扫）
│   │   ├── routes/
│   │   │   ├── public.js       参与者端接口
│   │   │   └── admin.js        后台接口（统计、明细、导出、设置）
│   │   ├── services/
│   │   │   ├── checkin.js      ★ 打卡提交（去重、空白过滤、幂等）
│   │   │   ├── stats.js        ★ 全部统计口径
│   │   │   ├── export.js       CSV 导出 + 附件打包 zip（零依赖手写 zip）
│   │   │   ├── participant.js  登记/找回/我的记录
│   │   │   ├── magic.js        文件头校验（防止改后缀传木马）
│   │   │   └── settings.js     活动配置（数据库覆盖 .env）
│   │   └── storage/
│   │       ├── index.js        存储驱动工厂 + objectKey 命名规范
│   │       ├── local.js        本机磁盘驱动
│   │       └── qiniu.js        七牛云驱动
│   ├── db/
│   │   ├── schema.sql          建表 SQL（8 张表）
│   │   ├── tasks-data.js       ★ 49 项任务字典（唯一真源）
│   │   ├── init.js             建库建表 + 灌任务 + 建管理员
│   │   ├── demo-data.js        造/清演示数据
│   │   ├── reset.js            ★ 清空业务数据（保留任务字典与管理员）
│   │   └── grant.sql           数据库账号授权 SQL（已执行过）
│   ├── test/
│   │   ├── smoke.js            接口验收（49 项）
│   │   ├── stats-audit.js      统计口径与独立 SQL 对账（58 项）
│   │   ├── browser-check.js    真浏览器端到端验收（44 项，出截图）
│   │   └── debug-submit.js     定点调试脚本（怀疑提交链路有问题时用）
│   └── .env                    配置（数据库、存储、CORS）
└── web/                        前端
    └── src/
        ├── views/              参与者端 5 个页面
        │   └── admin/          后台 7 个页面
        ├── components/
        │   └── MediaUploader.vue  ★ 照片必填/视频选填的上传组件
        ├── styles/
        │   ├── outseta.css     ★ 参与者端皮肤（暖阳渐变）
        │   └── planpoint.css   ★ 后台皮肤（蓝图白）
        └── utils.js            图片压缩、上传进度、格式化
```

---

## 三、本地起服务（现在就能跑）

数据库和表都已就绪，不需要再做任何初始化。

```bash
# 1. 打包前端（改了前端代码才需要重跑）
cd 打卡工具/web
npm run build

# 2. 启动后端（会自己托管 web/dist）
cd ../server
npm start
```

打开 `http://127.0.0.1:3000`：
- 家长入口 `http://127.0.0.1:3000/`
- 管理后台 `http://127.0.0.1:3000/admin`（`admin / admin123456`）

### 如果要把数据库搬到别的机器 / 重装

```bash
cd 打卡工具/server
npm run db:init      # 幂等，重复执行只更新不删除
```

它会：建 8 张表与索引 → 灌 49 项任务 → 建管理员 → 写 15 项默认配置。

### ★ 演练模式（上线前彩排用）

今天是 9/15，不在活动期（10/1–10/7），直接跑会一直提示"活动还没开始"。
所以后端做了一个**显式开关**的模拟日期，只在开发环境生效：

```bash
cd 打卡工具/server
DAKA_ALLOW_CLOCK_OVERRIDE=1 DAKA_TODAY=2026-10-01 npm start
```

- 全站「今天」会变成 10/1，任务卡、统计、去重全部按 10/1 走
- 家长端和后台都会出现**黄色演练横幅**，不会被误当成真实数据
- `NODE_ENV=production` 时这个开关**直接失效**，不存在上线忘了关的风险
- 想彩排 10/3 就写 `DAKA_TODAY=2026-10-03`，可以一天一天过一遍

---

## 四、三套验收脚本（改了代码就重跑一遍）

```bash
cd 打卡工具/server

# 1) 接口层：注册/上传/提交/去重拦截/空白拦截/记录查询/全部统计接口/导出
node test/smoke.js --admin-pass=admin123456

# 2) 统计口径：把后台每个 KPI 和独立写的原始 SQL 对一遍，防止"数对不上"
node test/stats-audit.js --admin-pass=admin123456

# 3) 浏览器端：真 Chrome 跑完整流程，出 20 张截图到 server/shots/
#    （需要本机装了 Chrome；路径依赖 playwright，见下方注意事项）
node test/browser-check.js --admin-pass=admin123456
```

第三套最值钱——它会真的：登记 → 刷新页面确认登录态还在 → 选任务 → 上传照片 →
点提交 → 确认 `POST /api/checkin` 真的发出去了 → 查记录 → 看战报 → 登录后台逐页翻 →
真实下载一次 CSV 并检查有没有 UTF-8 BOM。**只看截图会误判成"没反应"，所以它断言的是网络请求。**

跑的时候要绕过本机代理（这台机器设了 `https_proxy=127.0.0.1:50232`）：

```bash
NODE_PATH="C:/Users/liwei/.workbuddy/binaries/node/workspace/node_modules" \
  node test/browser-check.js --admin-pass=admin123456
```

---

## 五、数据清理（★ 上线前必做）

现在库里混着 **200 人演示数据 + 若干"验收xxx"测试账号**。正式活动开始前清一次：

```bash
cd 打卡工具/server

# 全部清空（保留任务字典、管理员、活动设置）
node db/reset.js            # 不带参数会问一次，输入 yes 继续
node db/reset.js --yes      # 跳过确认

# 只清某一天（比如彩排把 10/2 的数据弄脏了）
node db/reset.js --date=2026-10-02 --yes
```

清完会打印一份前后对比：

```
  清空结果：
    参与者  212 → 0
    提交批次 1608 → 0
    打卡明细 1520 → 0
    凭证台账 3240 → 0
    任务字典 49 条（未动）
    磁盘文件 删除 3240/3240 个，释放 78.42MB
```

`local` 驱动下它会顺手把磁盘上的凭证文件也删掉；切到七牛后它只提示 key 数量，
云文件请到七牛控制台按前缀 `daka/` 批量清理。

> 只想清演示数据、保留手工建的真实账号时，也可以用
> `node db/demo-data.js --clear`（只删特定号段的演示账号）。

---

## 六、微信里怎么打开（上线必须解决）

微信内置浏览器**要求 HTTPS**，且 `http://` 下的视频基本播不了。
所以正式分发前需要一张公网 HTTPS 证书。

### 推荐做法：整台服务跑在云服务器上

数据库已经在 `120.79.240.81` 了，最省事的方案是把 Koa 也部署到同一台机器：

1. 服务器上装 Node 18+，把 `打卡工具/` 传上去
2. `cd web && npm install && npm run build`
3. `cd ../server && npm install`，用 pm2 守护：`pm2 start src/index.js --name daka`
4. Nginx 反向代理 + HTTPS：

```nginx
server {
    listen 443 ssl http2;
    server_name daka.example.com;

    ssl_certificate     /etc/nginx/cert/daka.pem;
    ssl_certificate_key /etc/nginx/cert/daka.key;

    client_max_body_size 40m;   # 必须 ≥ 视频上限（默认 30MB）+ 余量

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;   # 传大视频时别被 nginx 掐断
    }
}
server {
    listen 80;
    server_name daka.example.com;
    return 301 https://$host$request_uri;
}
```

5. 改 `server/.env` 里的 `CORS_ORIGINS`（同源部署可保持 `*`，或写成 `https://daka.example.com`）

### 分发方式

- 家长入口：`https://daka.example.com/`
- 管理后台：`https://daka.example.com/admin`（**不要**发到群里）
- 用草料二维码等工具把家长入口生成二维码，印在签到台/打卡点展板上

---

## 七、切到七牛云（凭证还没给，填上即可）

本机磁盘驱动适合彩排，但 1000 人 × 7 天的照片视频放在应用服务器磁盘上
会撑爆机器，也会把服务器带宽吃光（家长每看一次记录都要回你的服务器拉图）。

**代码已经两条路都写好了**，切换只改 `.env`，前端一行都不用动：

```ini
# server/.env
STORAGE_DRIVER=qiniu
QN_ACCESS_KEY=你的AccessKey
QN_SECRET_KEY=你的SecretKey
QN_BUCKET=你的空间名
QN_REGION=z0            # 华东 z0 / 华北 z1 / 华南 z2
QN_DOMAIN=cdn.example.com
QN_PRIVATE=1            # 私有空间填 1，后端会签 30 分钟有效的临时链接
```

重启后端，访问 `/api/health`，看到 `"storage":{"driver":"qiniu","qiniuReady":true}` 即生效。

### 七牛控制台还要配三处

1. **新建空间** → 建议**私有空间**（未成年人照片别裸奔），后端会签临时链接
2. **跨域设置（CORS）** → `Access-Control-Allow-Origin` 填你的家长入口域名，否则浏览器直传会被拦
3. **绑定已备案域名并开 HTTPS** → 测试域名仅 HTTP、30 天有效，iOS 微信里视频可能放不了

### 上传路径的差异（已封装，你不用管）

| | 前端行为 | 服务器带宽占用 |
|---|---|---|
| `local` | 先领凭证 → 传到我们的 `/api/upload/local` → 后端落盘 | **全部占用**（视频也走服务器） |
| `qiniu` | 先领凭证 → **直传七牛**，服务器只签一个 token | 几乎为零 |

### 成本提醒

七牛免费额度是 **10GB 存储 + 10GB/月 CDN 流量**。
按 1000 人 × 7 天、照片压到 0.4MB 估算，照片约 7GB，
若 20% 用户额外传 10MB 视频就再加 14GB —— **一定超**。

三种应对（按推荐度排）：
1. **不收视频，视频凭证走微信群** —— 最省事、最不容易翻车
2. 视频上限压到 20MB 且限制 60 秒 —— 存储约 10GB，刚好压在免费线上
3. 升级付费版扩容（有费用）

**每日导出+清理必须做**（见第八节）。

---

## 八、每日运维（3 分钟，必做）

活动期间每天活动结束后：

1. 打开后台 → **附件与空间**
2. 「导出附件」选当天日期 → 打包 zip 下载存档（存到网盘或移动硬盘）
3. 「清理未提交的文件」→ 先「试算」看有多少 → 再「确认清理」
4. 顺手看一眼「打卡明细」有没有异常（比如某人 1 分钟提交 20 条）

清理只删「从未绑定到任何打卡记录」的文件，**不影响已归档的凭证**。
这一步是能不能撑住 7 天的关键。

---

## 九、数据口径（口径统一，不用各处理解）

| 指标 | 算法 | 说明 |
|---|---|---|
| **累计打卡次数** | `COUNT(*) FROM daka_checkin` | 每完成 1 项任务算 1 次。一次提交 3 项就是 3 次 |
| **累计打卡人数** | `COUNT(DISTINCT participant_id)` | 至少完成过 1 次的人 |
| **当日打卡次数/人数** | 同上，加 `checkin_date = ?` | 按服务端北京日期 |
| **当日最多项目** | 当天各任务 `COUNT` 的最大值 | 可能并列，所以返回数组 |
| **当日最少项目** | 当天各任务 `COUNT` 的最小值 | 同上 |
| **全能少年** | `COUNT(DISTINCT theme) = 7` | 7 个主题每个至少 1 次 |
| **打卡达人** | `COUNT(*) >= 14` | 不限主题，理论上限 49 次 |
| **主题之星** | 单主题 `COUNT(*) >= 3` | 因为每天同一主题任务不同，所以 3 次 = 3 天 |
| **重名判定** | 同名 + 同校 + 同手机 | 三者全同才算"疑似重复登记"，避免误伤真重名 |
| **线下打卡点记录** | `is_offline = 1` | 10/2–10/6 各有 1 项 |

**重名问题怎么解决的**（需求第 6 条）：

唯一标识是自增 `id`，姓名从来不是身份。业务身份用 `(手机号, 姓名)` 唯一键：

- 同一家长给两个孩子报名 → 同手机号 + 不同姓名 → 两条记录，`siblings` 字段会提示
- 两个孩子重名但在不同家庭 → 不同手机号 + 同姓名 → 两条记录，靠手机号区分
- 同一家长重复登记同一个孩子 → 命中唯一键 → 直接返回已有记录，不新增
- 前端会在登记成功后提示"系统里有 N 位同名的孩子，分别来自 X、Y"，并说明"这不是错误"
- 公开战报的排行榜姓名做脱敏（张\*三），后台明细给全名

---

## 十、后台功能

| 页面 | 能干什么 |
|---|---|
| **总览** | 累计次数/人数/当日数据/人均，当日最多最少项目，七项排行，七天趋势，日期×主题矩阵，学校排行，打卡天数分布，主题覆盖分布，资源占用，重名提醒。可切换日期查看任意一天 |
| **打卡明细** | 按日期/主题/学校/姓名手机号/线下任务筛选，看凭证（照片+视频），删除单条（家长传错时可救），导出 CSV |
| **参与者** | 名单+每人累计/天数/主题覆盖/荣誉标签，只看"已登记未打卡"用于催打卡，点姓名看该人全部记录，导出名单 |
| **荣誉名单** | 三类荣誉名单分栏展示，按姓名/学校筛选，导出 |
| **附件与空间** | 空间占用概览、孤儿文件数、按日期导出 zip、清理孤儿文件、附件清单 CSV |
| **任务字典** | 49 项任务只读展示 + 每项已打卡次数（见"已知限制第 1 条"） |
| **活动设置** | 改活动文案/公告、开关打卡、调上传上限与荣誉门槛，**保存即生效不用重启**；含后台操作日志 |

导出格式：6 种 CSV（打卡明细/参与者汇总/每日主题矩阵/荣誉名单/附件清单/重名清单）+ 附件打包 zip。
CSV 带 UTF-8 BOM，Excel 双击直接打开不乱码。

---

## 十一、需求逐条对照

| # | 要求 | 实现 |
|---|---|---|
| 1 | 一天内可选 1 个或多个主题打卡；可多次提交但不重复做同一主题 | 勾选式多选，只勾 1 个提交也行，勾多个集中提交也行。`uk_once(participant_id, task_id)` 从数据库层拦住重复 |
| 2 | 用户提交后可自行查看提交记录 | `/records` 页按日期倒序分组，带照片缩略图可放大、视频可播、每项显示自己的说明文字 |
| 3 | 活动方快捷统计累计次数、人数、当日最多/最少项目 | 后台总览页一组 KPI + 当日冷热双卡，全部实时计算，无冗余字段所以不会"对不上" |
| 4 | 部分统计可直接呈现给参与者 | `/board` 打卡战报：累计数据、今日冷热、七项分布、七天趋势、脱敏排行榜。后台可一键关闭 |
| 5 | 收集图片和视频，照片必填视频选填 | 服务端按 `media` 数组里 `type=image` 是否为空独立判定，视频传不传都行 |
| 6 | 预计 1000 人，需考虑重名 | 见第九节。唯一标识用 id，业务身份 `(手机号,姓名)`，前端主动提示同名情况 |
| 7 | 活动说明／信息收集（只填一次）／日期任务选择／上传凭证 | 四个页面：`/` 活动说明 → `/register` 一次登记（姓名·学校班级·联系方式，明说是证书凭证）→ `/checkin` 今日任务+上传 → 之后每天直接进 `/checkin` |
| 8 | 重复提交或空白提交自动过滤 | 四种跳过原因码：`DUPLICATE`（重复）、`NO_PHOTO`（没照片）、`NOT_TODAY`（不是今天的任务）、`BAD_MEDIA`（文件失效）。前端会清楚列出哪几项没算上、为什么 |
| 9 | 全程免费、微信里方便打开 | 代码零授权成本；MySQL + 七牛免费额度 + 自建服务。移动端优先布局，吸底提交条、图片自动压缩、上传进度条、`requestId` 幂等防微信重发。**唯一需要花钱的是域名与 HTTPS 证书**（约几十元/年） |

### 额外加上的（原需求没提但会用得上）

- **服务端定日期**：不信客户端传的 date，防止改手机时间把 10/8 的打卡记到 10/3
- **`requestId` 幂等**：微信里网络抖动导致重发 POST，直接回放上次结果，不产生重复数据
- **图片客户端压缩**：手机直出 4–8MB，压到长边 1600px / 画质 0.82，通常剩 300–600KB。HEIC 无法解码时直接原图上传，宁可不压也不能让人传不上去
- **上传先落台账**：先写 `daka_media`（status=0）再发上传凭证，所以"传了但没提交"的孤儿文件是**可枚举的**，不靠时间戳猜
- **文件头校验**：读文件前 16 字节核对真实类型，改后缀名传别的文件会被拦
- **临时公告**：后台随时改打卡页顶部提示，应对线下点临时改地点这类情况
- **一键关打卡**：数据异常时可先关掉提交入口再排查
- **操作日志**：删记录、改配置、清文件全部留痕，谁干的能查到

---

## 十二、已知限制

1. **任务字典不开放后台编辑**。每条打卡记录都抄了一份 `theme`/`task_name` 快照，改字典会让老数据对不上。要改请编辑 `server/db/tasks-data.js` 后重跑 `npm run db:init`（幂等，只更新不删除）。
2. **日期题不能限制"只能选今天"**，靠的是「每天只有一个表单页 + 当天发当天链接」——本工具天然如此，因为任务字典按日期绑定，昨天和明天的任务在今天的页面上根本不可选。
3. **七牛免费额度撑不住 1000 人全量收视频**，见第七节的成本估算。
4. **需要公网 HTTPS** 才能在微信里正常使用，这是微信的要求不是工具的限制。
5. **单机部署**。1000 人在单台 2 核 4G 服务器上够用（典型峰值 200 QPS 左右，MySQL 连接池 10 够）；要横向扩进程时把限流从内存换成 Redis 即可，接口不用改。
6. **家长端首屏标题在窄屏的换行已调好**（430px 宽下从"最后一个字单独一行"改成整行显示），
   但如果后来改了活动标题文案、字数变多，需要回头看一眼还放不放得下。
7. **数据库 3306 直接暴露在公网**。现在是靠强密码 + 只授权 `daka` 库撑着。活动结束后建议在云服务器安全组里把 3306 的来源 IP 收紧，或只允许应用服务器访问。

---

## 十三、踩过的坑（改代码前先看这个）

### 坑 1：本机代理会让 curl 打不通本地服务
这台机器设了 `https_proxy=http://127.0.0.1:50232`，`curl http://127.0.0.1:3000` 会被
代理劫走然后返回 400/超时，看起来像"服务没起来"。
排查时加 `--noproxy '*'`：`curl --noproxy '*' http://127.0.0.1:3000/api/health`。
Node 的 `fetch` 不受影响，所以测试脚本本身不用改。

### 坑 2：上传临时目录不存在 → 上传报 500 且毫无线索
`koa-body` 配的 `formidable.uploadDir` 如果目录不存在，formidable v2 的写流会**静默失败**：
multipart 解析照样返回成功、`file.filepath` 照样有值，然后读文件时炸一个没头没尾的 500。
已在 `src/index.js` 里**启动时强制创建**该目录，并且上传接口会先 `stat` 一次确认文件真的在盘上，
不在就回一句"文件没有传完整，请检查网络后重新上传"。

### 坑 3：写自动化测试时别用 `button:has-text("提交")` 当选择器
任务卡里那个取消勾选的按钮文案是「已加入本次**提交**」，按 DOM 顺序会先于吸底条的
「提交打卡」按钮被命中——一点就把刚选中的项取消了，然后测试报"提交没反应"，其实是测试自己的锅。
正确写法：`.o-stickybar button.o-btn--primary`。

### 坑 4（问卷网遗留，供对照）：上传类题目在问卷网无法做成选填
这属于上一版方案的结论，记在这里免得以后又绕回去：问卷网的照片/文件题把"没文件"
判定为文件无效，`is_required` 也不在服务端结构里（读接口全都不返回，显式写 0 也会被拦）。
本工具之所以要自建，这是主因。

---

## 十四、常见问题

**Q：家长说"打开是空白页"？**
A：先看 `https://域名/api/health`，`db.ok` 是不是 true。若 false 是数据库授权掉了；若整个打不开是 nginx 或服务挂了。

**Q：家长说"提交了但没算上"？**
A：让他点"我的记录"看看有没有。没有就是被过滤了，后台「打卡明细」按姓名搜一下，能看到真实情况。最常见的两种：照片没传成功（网差）、同一个主题今天已经打过。

**Q：家长说"第二天打开要我重新登记"？**
A：这个 bug 已经修掉了（验收脚本里有一条★回归断言专门盯它）。原因是判断"是否已登记"用了
"内存里的用户信息已加载"而不是"本地有登录凭证"，刷新页面就会误判成没登记。
如果还遇到，让家长换个浏览器（微信里清一下缓存）再试。

**Q：两个孩子能不能用同一个手机号？**
A：可以。姓名不同即视为两个孩子，系统分别统计。

**Q：能不能补打卡？**
A：不能，服务端按北京日期判定。如果确实要开例外，只能后台手动往数据库插（不建议，会让口径混乱）。

**Q：演示数据忘了清怎么办？**
A：`node db/reset.js --yes` 全清，或 `node db/demo-data.js --clear` 只清演示账号。

**Q：怎么改后台密码？**
A：登录后右上角「改密码」。忘了密码就在服务器上跑 `node db/init.js --admin-only` 重置。

**Q：彩排时忘了关演练模式，上线会不会有问题？**
A：不会。`NODE_ENV=production` 时 `DAKA_ALLOW_CLOCK_OVERRIDE` 直接失效，只会按真实日期走。
